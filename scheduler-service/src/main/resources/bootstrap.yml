spring:
  application:
    name: hrss-scheduler-service
  profiles: 
    active: development
  cloud: 
    config:
      uri: http://configuration-service:9090
      
management:
  security:
    enabled: false